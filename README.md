# Daily Tracker

My first Python project.

This app tracks:
- coding days
- streaks
- progress logs

Built while learning Python and GitHub.

## Run it

```bash
python3 daily-tracker.py
```

Press Enter at the day prompt to use today's date automatically. The app accepts
`yes`, `y`, `no`, or `n` and saves each entry to `log.txt`.
